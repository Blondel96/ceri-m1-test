machine:
  java:
    version: oraclejdk8
 
test:
    override:
        - mvn test
        - mvn jacoco:report
general:
    branches:
        only:
            - master # list of branches to build